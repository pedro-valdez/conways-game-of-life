export default function ExplanationBody() {
	return (
		<p>Conway&aposs Game of Life, often simply called Life, takes place in a two-dimensional grid of alive or dead cells. Life has rules comparable to death, survival and reproduction, hence the name of the game. The rules are:</p>
	)
}
